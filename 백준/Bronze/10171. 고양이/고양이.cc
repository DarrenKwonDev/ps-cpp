#include <iostream>

using namespace std;

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);

    string a = R"(\    /\
 )  ( ')
(  /  )
 \(__)|
    )";

    cout << a;

    return 0;
}